import { OS_DesignComponents } from '../OS_DesignComponents';

export default {
    title: 'OS Design Components',
    component: OS_DesignComponents,
};

const Template = args => < OS_DesignComponents />;

export const Default = Template.bind({});